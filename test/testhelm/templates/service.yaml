apiVersion: v1
kind: Service
metadata:
  name: {{ include "signuphelm.fullname" . }}
  labels:
    {{- include "signuphelm.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "signuphelm.selectorLabels" . | nindent 4 }}

apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "signuphelm.fullname" . }}-test-connection"
  labels:
    {{- include "signuphelm.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "signuphelm.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
